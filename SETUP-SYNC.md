# スマホとパソコンで内容を共有する（同期）セットアップ

このアプリのデータは、これまで**その端末のブラウザの中だけ**に保存されていた。
だからパソコンで書いた内容はスマホには出てこなかった。

そこで、自分のGoogleドライブに「共通の保管場所」を作り、両方の端末がそこを見に行くようにした。

**アプリの中に手順が組み込まれている。** 基本はそれに従えばいい。
この文書は、詰まったときに読む用。

---

## いちばん短い道順

パソコンのアプリを開いて、

**システム設定** → **端末間の同期** → **セットアップを始める**

あとは画面の1〜6に従うだけ。所要 5〜10分。

そのうち、アプリが代わりにやってくれる部分：

- 合言葉（データの鍵）は**ボタンひとつで生成**される。自分で考えなくていい
- スクリプトは**合言葉を埋め込んだ状態でコピー**される。**コードを手で書き換える必要はない**

あなたがやる必要があるのは、Googleの画面での操作だけ。
ここはGoogleアカウントの承認が必要なので、代行できない。

1. script.google.com で「新しいプロジェクト」を作り、コピーしたコードを貼る
2. `setup` を実行して権限を承認する
3. 「デプロイ → ウェブアプリ」で公開し、URLをコピーする
4. アプリに戻ってURLを貼り、「保存して同期」

### 2台目（スマホ）は、ずっと簡単

**手順1〜5は不要。** スマホでアプリを開いて同じセットアップ画面に行き、

- パソコンで使った**合言葉**（設定画面の「合言葉をコピー」で取れる）
- 同じ**URL**

この2つを入れて「保存して同期」するだけ。

> **順番に注意。** 必ず**データが入っているパソコン側から先に**やること。
> 先に空のスマホを同期すると、空の状態が保管場所に上がってしまう。

---

## 大事な後始末（これは必ずやる）

以前のバージョンでは、GASのURLと合言葉 `jonsonpanson` が
**公開リポジトリのコードに直接書かれていた**。誰でも読める状態だった。

今回コードからは消したが、**それだけでは漏れた分は無効にならない。**
古いデプロイが生きている限り、URLと合言葉を知っている人は書き込める。

やること：

1. https://script.google.com/ で**古いプロジェクト**（`AKfycbzCQPNsL18...` のデプロイを持つもの）を開く
2. 「**デプロイ**」→「**デプロイを管理**」
3. 古いデプロイの右にあるメニューから「**アーカイブ**」または「**削除**」

これで古いURLは死ぬ。新しいセットアップは別プロジェクトなので影響しない。

---

## 使い方

- 設定が済めば、あとは意識しなくていい。
  変更するたびに自動で送られ、アプリを開いたとき・別アプリから戻ってきたときに自動で受け取る。
- 左メニュー下、またはスマホ上部のアイコンで**今すぐ同期**もできる。
- アイコンの色： 緑＝同期済み／黄＝未送信あり／赤＝エラー

## 保存されるもの

Googleドライブに `営業参謀Pro` フォルダができて、その中に：

| 場所 | 中身 |
|---|---|
| `sales-atelier-state.json` | 同期の本体。全データ |
| `バックアップ/` | 1日1本、14日分の自動バックアップ |
| `morning/` `night/` `warehouse/` | 朝の作戦・夜の報告・知識の倉庫の書き出し（.md） |
| `ログ/` | 動作ログ（月ごと） |

## 覚えておくこと

- **APIキーは同期されない。** 端末ごとに設定画面で入力する（鍵を持ち歩かないための仕様）。
- **同じデータを両方の端末で同時に書き換えると「競合」になる。**
  そのときはどちらを残すか聞かれる。丸ごとどちらかを選ぶ方式で、
  行単位の自動マージはしない。
- **誤って上書きしても戻せる。** `バックアップ/` に14日分ある。

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| 「同期先から想定外の応答が返ってきた」 | デプロイ設定の「アクセスできるユーザー」が「全員」になっていない。または URL が `/exec` で終わっていない |
| 「スクリプト側の SYNC_TOKEN がまだ初期値のまま」 | 貼り付けたコードが古いか、貼り付けに失敗している。セットアップ画面の手順2からやり直すのが早い |
| 「合言葉が違う」 | 2台目で違う合言葉を入れている。1台目の設定画面「合言葉をコピー」から取り直す |
| コードを直したのに反映されない | GAS はデプロイし直さないと反映されない。「デプロイ」→「デプロイを管理」→ 鉛筆アイコン → バージョン「新バージョン」→ デプロイ |
| スマホだけ同期されない | 省電力モードでバックグラウンド通信が止まっていることがある。アプリを開いて同期アイコンを押す |
| 自動コピーができない | クリップボードが使えない環境。全文が表示されるので、それを選択してコピーする |

---

## スマホでの使い方（ホーム画面に置く）

- **iPhone (Safari)**： 共有ボタン →「ホーム画面に追加」
- **Android (Chrome)**： 右上メニュー →「ホーム画面に追加」

アドレスバーのない全画面で開くようになる。

---

## 開発者向け

`gas/sync.gs` が正。アプリ内のコピーボタン用に、同じ内容が
`index.html` の `<script type="text/plain" id="gas-source">` に同梱されている。

```bash
python3 gas/embed_gas.py     # gas/sync.gs を index.html に埋め込み直す
python3 gas/verify_embed.py  # 両者が一致しているか確認する
```

`gas/sync.gs` を編集したら `embed_gas.py` を実行すること。

# スマホとパソコンで内容を共有する（同期）セットアップ

このアプリのデータは、これまで**その端末のブラウザの中だけ**に保存されていた。
だからパソコンで書いた内容はスマホには出てこなかった。

そこで、自分のGoogleドライブに「共通の保管場所」を作り、両方の端末がそこを見に行くようにした。
以下は一度だけやる作業。所要 5〜10分。

---

## 手順

### 1. Google Apps Script のプロジェクトを作る

1. https://script.google.com/ を開く（アプリで使っているGoogleアカウントでログイン）
2. 左上「**新しいプロジェクト**」
3. 最初から入っている `function myFunction() {}` を全部消す
4. このリポジトリの **`gas/sync.gs`** の中身を全部コピーして貼り付ける

### 2. 合言葉を決める

貼り付けたコードの上のほうにこの行がある。

```javascript
var SYNC_TOKEN = 'CHANGE_ME_your_secret_passphrase';
```

`CHANGE_ME_your_secret_passphrase` を、**自分にしか分からない文字列**に書き換える。
英数字で12文字以上を推奨（例：`kamon-2026-shitsumu-x7`）。

> これがデータの鍵になる。誕生日や名前など推測されやすいものは避ける。

### 3. 権限を先に通しておく

1. エディタ上部の関数選択で `setup` を選ぶ
2. 「**実行**」を押す
3. 「承認が必要です」→ 自分のアカウントを選ぶ
4. 「このアプリは確認されていません」と出たら
   「**詳細**」→「**（プロジェクト名）に移動**」→「**許可**」

### 4. ウェブアプリとして公開する

1. 右上「**デプロイ**」→「**新しいデプロイ**」
2. 歯車アイコン →「**ウェブアプリ**」を選ぶ
3. 設定は次の通り
   - 次のユーザーとして実行： **自分**
   - アクセスできるユーザー： **全員**
4. 「デプロイ」を押す
5. 表示される `https://script.google.com/macros/s/....../exec` を**コピー**

> 「全員」に不安を感じるかもしれないが、合言葉（SYNC_TOKEN）を知らない相手には
> データを一切返さない作りにしてある。URLと合言葉をセットで人に渡さないこと。

### 5. アプリ側に登録する（パソコン・スマホの両方で）

1. アプリを開く → 左メニュー下の「**システム設定**」
2. 「**端末間の同期（スマホ ⇄ パソコン）**」の欄に
   - 同期先URL： 手順4でコピーしたURL
   - 合言葉： 手順2で決めた文字列
   - この端末の名前： `パソコン` / `スマホ` など
3. 「同期を有効にする」にチェック
4. 「**接続テスト**」で「つながった」と出るか確認
5. 「**保存**」

**先にデータが入っている側（パソコン）から設定する**こと。
先に空のスマホを同期すると、空の状態が保管場所に上がってしまう。

---

## 使い方

- 設定が済めば、あとは意識しなくていい。
  変更するたびに自動で送られ、アプリを開いたとき・別アプリから戻ってきたときに自動で受け取る。
- 左メニュー下、またはスマホ上部のアイコンで**今すぐ同期**もできる。
- アイコンの色： 緑＝同期済み／黄＝未送信あり／赤＝エラー

## 覚えておくこと

- **APIキーは同期されない。** 端末ごとに設定画面で入力する（鍵を持ち歩かないための仕様）。
- **同じデータを両方の端末で同時に書き換えると「競合」になる。**
  そのときはどちらを残すか聞かれる。丸ごとどちらかを選ぶ方式で、
  行単位の自動マージはしない。
- **バックアップは自動で残る。** Googleドライブに
  `sales-atelier-state.backup-YYYY-MM-DD.json` が1日1本、14日分保存される。
  誤って上書きしても、ここから戻せる。

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| 「同期先から想定外の応答が返ってきた」 | デプロイ設定の「アクセスできるユーザー」が「全員」になっていない。または URL が `/exec` で終わっていない |
| 「合言葉が違う」 | GAS の `SYNC_TOKEN` と設定画面の合言葉が不一致。前後の空白にも注意 |
| コードを直したのに反映されない | GAS はデプロイし直さないと反映されない。「デプロイ」→「デプロイを管理」→ 鉛筆アイコン → バージョン「新バージョン」→ デプロイ |
| スマホだけ同期されない | 省電力モードでバックグラウンド通信が止まっていることがある。アプリを開いて同期アイコンを押す |

---

## スマホでの使い方（ホーム画面に置く）

- **iPhone (Safari)**： 共有ボタン →「ホーム画面に追加」
- **Android (Chrome)**： 右上メニュー →「ホーム画面に追加」

アドレスバーのない全画面で開くようになる。
